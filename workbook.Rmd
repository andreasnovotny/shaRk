---
title: "R Notebook"
output: html_notebook
---

# Add the dyntaxa datafile

```{r}
library(tidyverse)

dyntaxa <- read.delim("../../Barcoding_Project/Monitoring_data/Raw_data/dyntaxa/Biota(2).csv") %>% 
    select(TaxonId, Rike.1, Stam.1, Klass.1, Ordning.1, Familj.1, Släkte.1, Art.1)
colnames(dyntaxa) <- c("dyntaxa_id", "Kingdom", "Phylum", "Class", "Order", "Family", "Genus", "Species")
dyntaxa$dyntaxa_id <- as.character(dyntaxa$dyntaxa_id)
devtools::use_data(dyntaxa, overwrite = TRUE)

```

# Add documentation

```{r}
devtools::document()
```

# Add sample data

```{r}
library(shaRk)
zoo <- getSHARK("Zooplankton")
usethis::use_data(zoo, overwrite=TRUE)

data(zoo)

zoo

phyto <- getSHARK("Phytoplankton")
usethis::use_data(phyto, overwrite=TRUE)

```


